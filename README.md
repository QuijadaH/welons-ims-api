vscode
1. create python venv via vscode
2. move `requirements.txt` to vscode project folder
3. `pip install -r requirements.txt`

xampp
1. login to mariadb/mysql using root account (e.g. `/path/to/mysql -u root`)
2. create database `db_welons_v1`
3. import `db_welons_v1.sql` into `db_welons_v1`
